"""Canonical Streamlit entrypoint for local and cloud deployments.

Streamlit Community Cloud and similar platforms commonly default to `app.py`.
Importing `Simple` executes the existing app module and preserves the current UI.
"""

import Simple  # noqa: F401
